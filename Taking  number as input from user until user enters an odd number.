#include<stdio.h>
// Taking  number as input from user until user enters an odd number.
int main (){
   int n;
   do {
     printf("Enter n : ");
   scanf("%d",&n);
    printf("%d \n",n);
    if(n % 2 != 0){
        break;
    }
   } while(1);
   printf("Thank youu ");
    return 0;
}
