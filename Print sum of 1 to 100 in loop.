#include<stdio.h>
#include<math.h>

//Print sum of 1 to 100 in loop.

int main (){
    int sum = 0;
    for(int i = 1;i<=100; i++) {
        sum += i;
    }
    printf("sum is %d",sum);

    return 0;
}
