#include<stdio.h>

void increasingno(int x,int n){
    if(x>n){
        return;
    }
    printf("%d\n",x);
    increasingno(x+1,n);
    return;
}

int main () {
    int n;
    printf("Enter The Value of N : ");
    scanf("%d",&n);

    increasingno(1,n);


    return 0;
}
