#include<stdio.h>
#include<string.h>

// Print the full sentence in a string.

int main (){
    char sentence[100] ;
   gets(sentence);
 
  printf("The length of string is : %d",strlen(sentence));

    return 0;
}
