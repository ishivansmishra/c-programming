#include<stdio.h>
// Reverse table of number(n) given by user.
int main (){
    int n;
    printf("Enter n : ");
    scanf("%d",&n);

    for(int i = 10;i>=1;i--){
        printf("%d \n", n * i);
        
    }
    return 0;
}
