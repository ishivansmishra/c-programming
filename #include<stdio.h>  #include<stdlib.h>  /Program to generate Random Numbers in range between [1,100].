#include<stdio.h>

#include<stdlib.h>

// Program to generate Random Numbers in range between [1,100].

int main (){
    int a,n;

    printf("\n Ten Random Numbers in range[1,100] :: \n\n");

    for(a=1;a<=10;a++)
    {
         n = rand() % 100+1;
        printf("%d Random Number = %d\n",a,n);
    }

    return 0;
}
