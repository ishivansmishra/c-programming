#include<stdio.h>
// Print table of any number of loop.
int main (){
    int n;
    printf("Enter the number :  ");
    scanf("%d",&n);
    printf("\n");

    for(int i = 1 ; i<=10;i++){
        printf("%d*%d = %d \n",n,i,n*i);
    }

    return 0;
}
